━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  **DEEP DIVE — {project-name}**
  {date} · branch: {branch}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

**GIT STATE**
  branch:      {branch}
  uncommitted: {N files}
  unpushed:    {N commits}

**OPEN TODOs**  (tiered by priority, severity descending; tag-grouped within each tier)
  CRITICAL
    [BUG]   ⚠ {todo}
    [BUG]   ⚑ {todo} — possibly resolved by {hash}({repo}) — verify
  HIGH
    [BLOCKER] {todo}
  MEDIUM
    [DECISION] {todo}
    [CHORE]    {todo}
  LOW
    [INVESTIGATE] {todo}
  BACKLOG
    [CHORE] {todo}
  ── omit any empty tier; omit any tier header with no items ──
  ✓ {auto-resolved todo} (auto-resolved)   ← listed once at the bottom, not tiered

**GOTCHAS**
  · {gotcha}

**DECISIONS**
  · {decision — why}

**RE-ENTRY PROMPT**
┌────────────────────────────────────────────────────────────────────────────┐
  {paste this as first message}
└────────────────────────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
